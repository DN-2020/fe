import React from 'react';
import MainDividerPresenter from './MainDividerPresenter';

const MainDividerContainer = props => {
  return <MainDividerPresenter {...props} />;
};

export default MainDividerContainer;
