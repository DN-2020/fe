import React from 'react'
import SearchPresenter from "./SearchPresenter"
const SearchContainer = (props) => {
    return (
        <SearchPresenter props={props}></SearchPresenter>
    )
}

export default SearchContainer;
