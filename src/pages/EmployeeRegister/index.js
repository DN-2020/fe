import EmployeeRegisterContainer from "./EmployeeRegisterContainer";
import { connect } from "react-redux";

export default connect(
  ({ }) => ({

  }),
  dispatch => ({

  })
)(EmployeeRegisterContainer);
