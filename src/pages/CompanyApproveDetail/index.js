import CompanyApproveDetailContainer from "./CompanyApproveDetailContainer";
import { connect } from "react-redux";

export default connect(
  ({ }) => ({

  }),
  dispatch => ({

  })
)(CompanyApproveDetailContainer);
