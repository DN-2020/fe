import Company_Res_Container from "./Company_Res_Container"
import { connect } from 'react-redux';
import { actionCreators as ItemActions } from '../../redux/modules/item/actions';

export default connect(
  ({ }) => ({}),
  (dispatch) => ({})
)(Company_Res_Container);
